import 'package:flutter/material.dart';

class SearchJobScreen extends StatelessWidget {
  const SearchJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Search for Jobs')),
    );
  }
}
